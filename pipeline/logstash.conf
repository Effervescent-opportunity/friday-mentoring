input {
    tcp {
        port => "${logstash.port}"
        codec => json_lines
    }
}

output {
    elasticsearch {
        hosts => ["${ES_HOST:elasticsearch:9200}"]
        index => "logs-%{+YYYY.MM}"
        template_overwrite => true
    }
}